#include <stdio.h>

/**
 * main - programm entry point.
 * Description: uses printf to print a statement.
 * Return: 0 Indicates successful exec.
 */

int main(void)
	{
		printf("with proper grammar, but the outcome is a piece of art,\n");
		return (0);
	}
